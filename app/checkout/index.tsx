import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const CheckoutPage = () => {
    return (
        <View style={styles.container}>
            <Text>Checkout Page55</Text>
        </View>
    )
}

export default CheckoutPage

const styles = StyleSheet.create({
    container: {
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center',
    },
})
